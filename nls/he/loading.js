define(
	({
		// Note: this gives a JSHint error about a character being silently deleted, but it will have to be fixed
		// later
		/* jshint -W100 */
		loadingState: "טעינה...‏",
		errorState: "אירעה שגיאה"
	})
);
